# Sample data

Drop a small shapefile here to try the tool without touching your real
working data. A few hundred features is plenty for a demo.

```bash
cd sample_data
python ../shp_validity.py
```

Outputs land in `sample_data/cleaned/`, which is git-ignored, so demo
runs never get committed.
